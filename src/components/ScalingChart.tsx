// Legacy component — superseded by HeroChart / SeeingAxisChart / SizeAxisChart.
// Kept as a stub so any stale import does not break the build.
import type { XTCurve } from "@/lib/types";

export function ScalingPanel(_props: { curves: XTCurve[]; dropped: string[] }) {
  void _props;
  return null;
}
